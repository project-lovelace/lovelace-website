void bernoulli(int n, int* numerator, int* denominator) {
    // Your code goes here.

    // Store answers (instead of returning as the answer is made of two parts).
    *numerator = 0;
    *denominator = 0;
}

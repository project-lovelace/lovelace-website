const fs = require("fs");
const fileContents = fs.readFileSync("periodic_table.csv", "utf8");

function molecular_mass(chemical_formula) {
    let mass = 0;

    // Your code goes here!

    return mass;
}
